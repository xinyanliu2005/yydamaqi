/* ===================== CloudBase 环境配置 ===================== */
/* 从 CloudBase 控制台复制过来的环境 ID。这个值不是密钥，可以放心提交到公开
   仓库——真正的访问控制在数据库安全规则（见仓库根目录 cloudbase.rules.json）
   和云函数（cloudfunctions/applyGameAction/）里。
   把下面的占位符换成你自己环境的真实 ID 即可。 */
export var CLOUDBASE_ENV_ID = "REPLACE_ME";
